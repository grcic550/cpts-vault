---
title: "route"
sidebarTitle: "route"
description: "Commands and examples collected from your route notes."
keywords: ["route"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/route.md. Edit the Markdown source, then run scripts/sync_site.py. */}

[Search this tool’s saved examples](/commands/notebook?tool=route) · [All tools](/index)

## 4. Network Enumeration

**Notebook example**

```powershell
route print
```

Saved as written; may include lab values or terminal output.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

5 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="Read the interfaces and routes">

### Read the interfaces and routes

**Pivoting Fundamentals: Routes, SOCKS, and Forwards**

```powershell
ipconfig /all
route print
```

</Accordion>

<Accordion title="2 · Metasploit routing (pivot through a session)">

### 2 · Metasploit routing (pivot through a session)

**Pivoting, Tunneling, and Port Forwarding: Module Notes**

```bash
# through an existing meterpreter session:
run autoroute -s 172.16.5.0/23         # or: post/multi/manage/autoroute (autoadd)
route print                             # verify
# now any msf module reaching 172.16.5.x goes through the session
# expose those routes to EXTERNAL tools:
use auxiliary/server/socks_proxy        # SRVPORT 9050, VERSION 5, run -j
# then proxychains (socks5 127.0.0.1 9050) your nmap/xfreerdp/etc.
```

</Accordion>

<Accordion title="Network &amp; where you can go">

### Network & where you can go

**Windows Post-Exploitation: Field Reference**

```cmd
ipconfig /all                                :: interfaces (a 2nd NIC = a pivot like DMZ01)
arp -a                                       :: neighbors (other live hosts)
route print
netstat -ano                                 :: listening services + owning PID
```

</Accordion>

<Accordion title="Network Enumeration">

### Network Enumeration

**Windows Privilege Escalation: Full Checklist**

```powershell
route print
Get-NetRoute -AddressFamily IPv4 | ft DestinationPrefix,NextHop,RouteMetric,ifIndex
```

</Accordion>

<Accordion title="sshuttle: Route a Subnet over SSH">

### sshuttle: Route a Subnet over SSH

**sshuttle: Route a Subnet over SSH**

```bash
sshuttle -r ubuntu@pivot 172.16.5.0/23           # route that subnet over SSH
sshuttle -r ubuntu@pivot 172.16.5.0/23 -x pivot  # exclude the pivot itself
# now use tools normally — no proxychains:
nmap -sT -Pn 172.16.5.19
```

</Accordion>

</AccordionGroup>
