---
title: "smtp-user-enum"
sidebarTitle: "smtp-user-enum"
description: "Check SMTP username enumeration behavior."
keywords: ["smtp-user-enum"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/smtp-user-enum.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=smtp-user-enum) · [All tools](/index)

## Commands

**Kali**

```bash
smtp-user-enum -M VRFY -U users.txt -t TARGET_IP
smtp-user-enum -M RCPT -U users.txt -D DOMAIN -t TARGET_IP
```

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

3 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="USER Command">

### USER Command

**Attacking Email Services: Module Notes**

```text
grcic550@htb[/htb]$ smtp-user-enum -M RCPT -U userlist.txt -D inlanefreight.htb -t 10.129.203.7

Starting smtp-user-enum v1.2 ( http://pentestmonkey.net/tools/smtp-user-enum )

 ----------------------------------------------------------
|                   Scan Information                       |
 ----------------------------------------------------------

Mode ..................... RCPT
Worker Processes ......... 5
Usernames file ........... userlist.txt
Target count ............. 1
Username count ........... 78
Target TCP port .......... 25
Query timeout ............ 5 secs
Target domain ............ inlanefreight.htb

######## Scan started at Thu Apr 21 06:53:07 2022 #########
10.129.203.7: jose@inlanefreight.htb exists
10.129.203.7: pedro@inlanefreight.htb exists
10.129.203.7: kate@inlanefreight.htb exists
######## Scan completed at Thu Apr 21 06:53:18 2022 #########
3 results.

78 queries in 11 seconds (7.1 queries / sec)
```

</Accordion>

<Accordion title="Step 0: Enumerate valid usernames over SMTP">

### Step 0: Enumerate valid usernames over SMTP

**Read and Search Email with POP3 and IMAP**

```bash
# Automated with smtp-user-enum (tries each name and reports which the server accepts)
smtp-user-enum -M VRFY -U users.txt -t <IP>
smtp-user-enum -M RCPT -U users.txt -t <IP>   # fall back to RCPT if VRFY is disabled
smtp-user-enum -M RCPT -U users.txt -D domain -t <IP> #we can add -D for domain
```

</Accordion>

<Accordion title="Primary use: username enumeration">

### Primary use: username enumeration

**SMTP Enumeration and Relay Checks**

```bash
# Automated — smtp-user-enum tries each name and reports the hits
smtp-user-enum -M VRFY -U users.txt -t <IP>
smtp-user-enum -M RCPT -U users.txt -t <IP>     # fall back to RCPT if VRFY is disabled
smtp-user-enum -M EXPN -U users.txt -t <IP>

# Nmap equivalent
nmap -p25 --script smtp-enum-users,smtp-commands,smtp-open-relay <IP>
```

</Accordion>

</AccordionGroup>
