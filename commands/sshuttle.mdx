---
title: "sshuttle"
sidebarTitle: "sshuttle"
description: "Route selected traffic through an SSH host."
keywords: ["sshuttle"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/sshuttle.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=sshuttle) · [All tools](/index)

## Commands

**Kali**

```bash
sshuttle -r USER@PIVOT_IP INTERNAL_SUBNET
```

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

3 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="4. sshuttle">

### 4. sshuttle

**Pivoting and File Transfer: Field Reference**

```bash
sshuttle -r jbetty@<DMZ_IP> 172.16.119.0/24
# now use tools directly against 172.16.119.x
```

</Accordion>

<Accordion title="sshuttle: Route a Subnet over SSH">

### sshuttle: Route a Subnet over SSH

**sshuttle: Route a Subnet over SSH**

```bash
sshuttle -r ubuntu@pivot 172.16.5.0/23           # route that subnet over SSH
sshuttle -r ubuntu@pivot 172.16.5.0/23 -x pivot  # exclude the pivot itself
# now use tools normally — no proxychains:
nmap -sT -Pn 172.16.5.19
```

</Accordion>

<Accordion title="⏱️ DECISION HEADER — &quot;I need to… → do…&quot;">

### ⏱️ DECISION HEADER — "I need to… → do…"

**Pivoting, Tunneling, and Port Forwarding: Module Notes**

Context from the notebook: Scan the internal net  without proxychains sshuttle  (auto-routes over SSH) sshuttle -r user@pivot 172.16.5.0/23

```text
sshuttle -r user@pivot 172.16.5.0/23
```

</Accordion>

</AccordionGroup>
