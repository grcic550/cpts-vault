---
title: "sed"
sidebarTitle: "sed"
description: "Commands and examples collected from your sed notes."
keywords: ["sed"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/sed.md. Edit the Markdown source, then run scripts/sync_site.py. */}

[Search this tool’s saved examples](/commands/notebook?tool=sed) · [All tools](/index)

## 6. Convert the Ticket

**Notebook example**

```bash
sed 's/\$krb5tgs\$\(.*\):\(.*\)/\$krb5tgs\$23\$*\1*\$\2/' crack_file > sqldev_tgs_hashcat
```

Saved as written; may include lab values or terminal output.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

1 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="Key commands">

### Key commands

**Linux Remote Access: SSH, Rsync, and R-Services**

```bash
ssh -v user@<IP>                                   # verbose: shows offered auth methods
ssh -v user@<IP> -o PreferredAuthentications=password   # force password (for brute-force)
ssh -i id_rsa user@<IP>                            # key-based (chmod 600 the key first)
cat /etc/ssh/sshd_config | grep -v "#" | sed -r '/^\s*$/d'   # read server config (if on host)
```

</Accordion>

</AccordionGroup>
