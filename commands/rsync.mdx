---
title: "rsync"
sidebarTitle: "rsync"
description: "List modules or copy files through rsync."
keywords: ["rsync"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/rsync.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=rsync) · [All tools](/index)

## Commands

**Kali**

```bash
rsync rsync://TARGET_IP/
rsync -av --list-only rsync://TARGET_IP/MODULE
rsync -av rsync://TARGET_IP/MODULE ./received/
```

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

1 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="Rsync">

### Rsync

**Linux Remote Access: SSH, Rsync, and R-Services**

```bash
sudo nmap -sV -p 873 <IP>
nc -nv <IP> 873          # then send: @RSYNCD: 31.0  /  #list   to enumerate modules

rsync -av --list-only rsync://<IP>/<share>     # list a share's contents
rsync -av rsync://<IP>/<share> ./loot/          # download everything
rsync -av -e "ssh -p2222" rsync://<IP>/<share> ./loot/   # over SSH, custom port
```

</Accordion>

</AccordionGroup>
