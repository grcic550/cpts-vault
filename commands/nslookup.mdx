---
title: "nslookup"
sidebarTitle: "nslookup"
description: "Resolve a hostname using DNS."
keywords: ["nslookup"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/nslookup.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=nslookup) · [All tools](/index)

## Commands

**Kali**

```bash
nslookup MS01.DOMAIN DC_IP
nslookup -type=SRV _kerberos._tcp.DOMAIN DC_IP
```

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

1 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="DNS and Active Directory">

### DNS and Active Directory

**DNS Enumeration and Zone Transfers**

```bash
dig SRV _ldap._tcp.dc._msdcs.<domain> @<IP>        # locate domain controllers
nslookup -type=SRV _kerberos._tcp.<domain> <IP>    # locate the KDC (Kerberos)
```

</Accordion>

</AccordionGroup>
