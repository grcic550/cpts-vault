---
title: "Test-NetConnection"
sidebarTitle: "Test-NetConnection"
description: "Check whether a TCP service is reachable."
keywords: ["Test-NetConnection", "tnc"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/test-netconnection.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=test-netconnection) · [All tools](/index)

## Commands

**Windows PowerShell**

```powershell
Test-NetConnection -ComputerName MS01 -Port 445
```

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

1 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="PowerShell Remoting (WinRM) Transfers">

### PowerShell Remoting (WinRM) Transfers

**File Transfers for Linux and Windows**

```powershell
# Test connectivity
Test-NetConnection -ComputerName DATABASE01 -Port 5985

# Create a session
$Session = New-PSSession -ComputerName DATABASE01

# Copy file TO remote machine
Copy-Item -Path C:\samplefile.txt -ToSession $Session -Destination C:\Users\Administrator\Desktop\

# Copy file FROM remote machine
Copy-Item -Path "C:\Users\Administrator\Desktop\DATABASE.txt" -Destination C:\ -FromSession $Session
```

</Accordion>

</AccordionGroup>
