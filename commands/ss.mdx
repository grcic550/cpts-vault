---
title: "ss"
sidebarTitle: "ss"
description: "Show Linux listening sockets and connections."
keywords: ["ss", "Linux listening ports", "ss -tulpn"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/ss.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=ss) · [All tools](/index)

## TCP and UDP listeners

**Kali / Linux**

```bash
ss -tulpn
```

-t TCP, -u UDP, -l listening, -p processes, -n numeric addresses.

## Established TCP connections

**Kali / Linux**

```bash
ss -tpn state established
```

## If it fails

| Symptom | Check |
| --- | --- |
| Process name is missing | You may lack permission to inspect another user’s process. Re-run with sudo only when that access is available. |

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

3 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="6. Networking / pentest quick commands">

### 6. Networking / pentest quick commands

**Kali Terminal, tmux, zsh, and Vim**

```bash
ip a                # show interfaces & IPs (replaces ifconfig)
ip a show tun0      # just the VPN interface (HTB/CPTS uses tun0)
ip route            # routing table
ss -tulpn           # listening ports (replaces netstat)
ping -c 4 10.10.10.10
curl -s http://target/ | less       # fetch a page
curl -I http://target/               # headers only
wget http://target/file              # download a file
nc -lvnp 4444       # netcat listener (catch reverse shells)
python3 -m http.server 80            # quick web server to transfer files
```

</Accordion>

<Accordion title="Step 3 — Verify the tunnel is actually up">

### Step 3 — Verify the tunnel is actually up

**Pass the Ticket from Linux**

```bash
ss -tlnp | grep 1080        # should show something LISTENING on 127.0.0.1:1080
```

</Accordion>

<Accordion title="2. chisel (SOCKS + port forwards)">

### 2. chisel (SOCKS + port forwards)

**Pivoting and File Transfer: Field Reference**

Context from the notebook: R:  =  reverse  (pivot connects out to Kali — works through egress firewalls). Match the SOCKS port to proxychains. Verify it's up:  ss -tlnp | grep 1080 .

```text
ss -tlnp | grep 1080
```

</Accordion>

</AccordionGroup>
