---
title: "traceroute"
sidebarTitle: "traceroute"
description: "Commands and examples collected from your traceroute notes."
keywords: ["traceroute", "tracert"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/traceroute.md. Edit the Markdown source, then run scripts/sync_site.py. */}

[Search this tool’s saved examples](/commands/notebook?tool=traceroute) · [All tools](/index)

## Service & version detection (-sV) and OS detection (-O)

**Notebook example**

```bash
sudo nmap -sV <target>             # probe each open port for product + version
sudo nmap -O <target>              # guess OS from TCP/IP stack fingerprint
sudo nmap -A <target>              # aggressive: -sV + -O + default scripts + traceroute
```

Saved as written; may include lab values or terminal output.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)
