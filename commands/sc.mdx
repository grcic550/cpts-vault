---
title: "sc.exe"
sidebarTitle: "sc.exe"
description: "Query Windows service state, configuration and its security descriptor."
keywords: ["sc", "sc.exe", "service configuration", "service binary path"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/sc.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

## List services

**Windows — cmd.exe or PowerShell**

```cmd
sc.exe query state= all
```

Keep the space after state=.

## Inspect service configuration

**Windows — cmd.exe or PowerShell**

```cmd
sc.exe qc SERVICE_NAME
```

BINARY_PATH_NAME is the command line; SERVICE_START_NAME is the service account.

## Read service permissions

**Windows — cmd.exe or PowerShell**

```cmd
sc.exe sdshow SERVICE_NAME
```

Displays SDDL for the service object; the binary file has separate permissions.

## If it fails

| Symptom | Check |
| --- | --- |
| sc behaves like a file-writing command in PowerShell | Use sc.exe explicitly. In Windows PowerShell, sc can resolve to the Set-Content alias. |

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

[More saved commands for this tool](https://dashboard-aed21664.mintlify.site/find-anything?q=sc)

## Learning and original examples

- [Windows Privilege Escalation: Full Checklist](/privesc/windows-privesc-checklist)
