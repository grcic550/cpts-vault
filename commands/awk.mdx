---
title: "awk"
sidebarTitle: "awk"
description: "Extract fields or filter lines in text output."
keywords: ["awk", "field extraction"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/awk.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=awk) · [All tools](/index)

## First whitespace-separated field

**Kali**

```bash
awk '{print $1}' INPUT.txt
```

## Colon-delimited first field

**Kali**

```bash
awk -F: '{print $1}' INPUT.txt
```

## Unique nonempty lines

**Kali**

```bash
awk 'NF && !seen[$0]++' INPUT.txt > UNIQUE.txt
```

Write to a separate output file so you retain the original evidence.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

3 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="4. Reading &amp; filtering output (the pentest workhorses)">

### 4. Reading & filtering output (the pentest workhorses)

**Kali Terminal, tmux, zsh, and Vim**

```bash
cmd | grep foo                 # keep lines matching foo
cmd | grep -v foo              # drop lines matching foo
cmd | grep -E "80|443"         # match 80 OR 443 (regex)
cmd | awk '{print $1}'         # print 1st column
cmd | cut -d: -f1 /etc/passwd  # split on ":" take field 1
cmd | sort | uniq -c | sort -rn  # count & rank unique lines
cmd | wc -l                    # count lines
cmd | tee out.txt              # show on screen AND save to file
cmd > out.txt                  # save (overwrite)
cmd >> out.txt                 # append
cmd 2>/dev/null                # discard errors
cmd &>/dev/null                # discard everything
```

</Accordion>

<Accordion title="Kali / Linux">

### Kali / Linux

**awk for lab output**

```bash
awk '{print $1}' INPUT.txt
awk -F: '{print $1}' PASSWD_FILE
awk 'NF && !seen[$0]++' INPUT.txt > UNIQUE.txt
awk 'tolower($0) ~ /error/ {print NR ":" $0}' LOG.txt
```

</Accordion>

<Accordion title="Spawning Interactive TTY Shells">

### Spawning Interactive TTY Shells

**Shells, Payloads, and Interactive TTYs**

Context from the notebook: AWK awk 'BEGIN &#123;system("/bin/sh")&#125;'

```text
awk 'BEGIN {system("/bin/sh")}'
```

</Accordion>

</AccordionGroup>
