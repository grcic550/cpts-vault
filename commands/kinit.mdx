---
title: "kinit"
sidebarTitle: "kinit"
description: "Obtain a native Kerberos ticket and inspect the resulting cache."
keywords: ["kinit", "kdestroy", "native kerberos"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/kinit.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Replace UPPER_CASE values with your lab values.

[Search this tool’s saved examples](/commands/notebook?tool=kinit) · [All tools](/index)

**Requires:** Reachable KDC, correct realm, working name resolution and matching time.

## Password prompt

**Kali**

```bash
kinit USER@REALM
klist
```

Use your actual Kerberos realm, commonly uppercase. Native Kerberos needs appropriate realm/KDC discovery or configuration.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

2 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="Request a TGT Using the Keytab">

### Request a TGT Using the Keytab

**Pass the Ticket from Linux**

```bash
kinit carlos@INLANEFREIGHT.HTB -k -t carlos.keytab
```

</Accordion>

<Accordion title="Step 2 — Request a TGT with the keytab">

### Step 2 — Request a TGT with the keytab

**Pass the Ticket from Linux**

```bash
kinit 'LINUX01$@INLANEFREIGHT.HTB' -k -t /etc/krb5.keytab
klist     # should now show Default principal: LINUX01$@INLANEFREIGHT.HTB
```

</Accordion>

</AccordionGroup>

## Related tools and references

- [getTGT.py](/commands/gettgt)
