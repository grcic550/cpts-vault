---
title: "Active Directory Enumeration: Quick Reference"
sidebarTitle: "AD enumeration quick reference"
description: "Use a compact set of null-session, credentialed NetExec, and PowerView queries to begin mapping a domain."
keywords: ["ad enumeration", "null session", "enum4linux-ng", "nxc", "powerview", "local admin"]
---

{/* Generated from Active Directory/Reference/AD Enumeration Quick Reference.md. Edit the Markdown source, then run scripts/sync_site.py. */}

Supplementary quick reference from the existing vault site. It does not mark the module as complete.

> For the full module notes, use [initial enumeration](/active-directory/initial-enumeration), [Linux enumeration](/active-directory/enumeration-linux), [Windows enumeration](/active-directory/enumeration-windows), or the [command reference](/active-directory/command-reference).



With any domain foothold (even a low-priv user), enumerate before attacking. Get valid creds, then map the domain.

```bash
# From Linux, unauthenticated / low-priv
nxc smb $DC -u '' -p ''                       # null session
enum4linux-ng -A $DC
rpcclient -U '' -N $DC -c enumdomusers
# Authenticated sweep
nxc smb $DC -u USER -p PASS --users --groups --shares --pass-pol
nxc ldap $DC -u USER -p PASS --bloodhound -c all --dns-server $DC
```

```powershell
# From a domain-joined Windows host (PowerView)
Get-DomainUser | select samaccountname,description
Get-DomainComputer | select dnshostname,operatingsystem
Get-DomainGroupMember "Domain Admins"
Find-LocalAdminAccess       # where does my user have local admin?
```


> Collect with BloodHound early — it turns raw objects into the shortest path to Domain Admin. See the [BloodHound guide](/cheatsheets/bloodhound-complete-guide).
